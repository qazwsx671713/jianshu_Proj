1. 如何画一个三角形
    新建一个div
    设置h和w为0
    设置border: 10px solid transparent;
    看要什么三角形，设置其显示颜色
    border-bottom: 10px solid green;

############################################################
2. clearfix中，如果display是inline-block，那么会占一行空间。 为什么？
############################################################

3. 控制某个组件是否显示
    {condition && <Components/>}
    当condition为真，才显示

4. /store下新建actionCreator管理状态更新，实现状态更新与组件相分离

5. 安装并使用axios
    npm i axios
    import axios from 'axios'
    axios.get(...
        ).then(
                res=>{do something},
                err=>{do something}
                )

6. 适用redux-thunk实现异步action
    -1.安装redux-thunk
        npm i redux-thunk
    -2.使用redux-thunk
        在store.js中输入内容('-'代表删除代码，'+'代表增加代码):
            import reduxThunk from 'redux-thunk'
            import {applyMiddleware} from 'redux'
        -   const store = createStore(reducers)
        +   const store = createStore(reducers,applyMiddleware(reduxThunk));
    -3.使用thunk以后，我们就可以dispatch两种类型的参数
        (1).action对象
        (2).函数
    -4.当传入action对象
        此时直接执行reducer中的动作
    -5.当传入函数
        store会新建一个线程执行该函数。
        同时会给与该函数一个dispatch函数，方便当函数中的异步操作结束后更新数据
    -6. 代码模板:
        export function get_data(){
            return store.dispatch(
                (dispatch)=>{
                    data = ...（异步函数）
                    dispatch({
                        type:'...',
                        data:data
                    })
                }
            )
        }

7. 发现问题：热门搜索框聚焦时消失
    问题出现原因：热门搜索框由focus控制显示
    当点击热门搜索框时，出发onBlur,使得focus为false，热门搜索框消失
    解决方法：
        热门搜索框由focus与mouse_in共同控制
        当鼠标移入/移除热门搜索框时，改变mouse_in的状态。
        （即给热门搜索框绑定鼠标移入移出事件）
    
8. 每次点击搜索框都会获取热门搜索词，但是其实第一次获取就可以了
    判断搜索词list是否为空，为空时才发送ajax请求获取数据。